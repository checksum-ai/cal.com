import type { ChecksumLoginFunction } from "@checksum-ai/runtime";

export const login: ChecksumLoginFunction = async (page, { user, environment, config, payload }) => {
  console.log("TODO: login ");
};

export default login;
